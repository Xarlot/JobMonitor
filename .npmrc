# Refuse to install on a Node version that can't install Electron (see engines
# in package.json and .nvmrc). Without this, `npm install` on old Node fails deep
# inside Electron's postinstall with a cryptic ERR_REQUIRE_ESM.
engine-strict=true
