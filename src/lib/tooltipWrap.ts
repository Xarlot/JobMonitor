/**
 * Classes for a tooltip trigger wrapper. See the stylesheet for why these are descendant rules.
 */

import styles from './tooltipWrap.module.css';

export const tooltipWrap = styles.wrap;
export const tooltipWrapFixed = styles.wrapFixed;
